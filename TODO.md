# TODO
* A curated keybindings table.
* Install a subset of the terminfo files in xorg/usr/share/terminfo (what's required for alacritty and tmux).
* Workaround for the shitty GTK default menu (won't use a theme nor an XDG portal probably).
